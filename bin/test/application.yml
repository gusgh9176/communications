# Test
spring:
  profiles:
    active: local # 기본 환경 선택

# local 환경
---
spring:
  profiles: local
  jpa:
    show-sql: true
  data:
    mongodb:
      database: communications
      uri: mongodb://127.0.0.1:27017